#nullable enable
using Neatoo.RemoteFactory;
using Neatoo.RemoteFactory.Internal;
using Microsoft.Extensions.DependencyInjection;
using static RemoteFactory.UnitTests.FactoryGenerator.Save.SaveCodePathTests;
using RemoteFactory.UnitTests.Shared;
using RemoteFactory.UnitTests.TestContainers;

/*
    READONLY - DO NOT EDIT!!!!
    Generated by Neatoo.RemoteFactory
*/
namespace RemoteFactory.UnitTests.FactoryGenerator.Save
{
    public interface ISaveDeleteTargetFactory
    {
        SaveDeleteTarget Create(CancellationToken cancellationToken = default);
        SaveDeleteTarget? Save(SaveDeleteTarget target, CancellationToken cancellationToken = default);
    }

    internal class SaveDeleteTargetFactory : FactorySaveBase<SaveDeleteTarget>, IFactorySave<SaveDeleteTarget>, ISaveDeleteTargetFactory
    {
        private readonly IServiceProvider ServiceProvider;
        private readonly IMakeRemoteDelegateRequest? MakeRemoteDelegateRequest;
        // Delegates
        // Delegate Properties to provide Local or Remote fork in execution
        public SaveDeleteTargetFactory(IServiceProvider serviceProvider, IFactoryCore<SaveDeleteTarget> factoryCore) : base(factoryCore)
        {
            this.ServiceProvider = serviceProvider;
        }

        public SaveDeleteTargetFactory(IServiceProvider serviceProvider, IMakeRemoteDelegateRequest remoteMethodDelegate, IFactoryCore<SaveDeleteTarget> factoryCore) : base(factoryCore)
        {
            this.ServiceProvider = serviceProvider;
            this.MakeRemoteDelegateRequest = remoteMethodDelegate;
        }

        public virtual SaveDeleteTarget Create(CancellationToken cancellationToken = default)
        {
            return LocalCreate(cancellationToken);
        }

        public SaveDeleteTarget LocalCreate(CancellationToken cancellationToken = default)
        {
            return DoFactoryMethodCall(FactoryOperation.Create, () => new SaveDeleteTarget());
        }

        public SaveDeleteTarget LocalInsert(SaveDeleteTarget target, CancellationToken cancellationToken = default)
        {
            var cTarget = (SaveDeleteTarget)target ?? throw new Exception("SaveDeleteTarget must implement SaveDeleteTarget");
            return DoFactoryMethodCall(cTarget, FactoryOperation.Insert, () => cTarget.Insert());
        }

        public SaveDeleteTarget LocalUpdate(SaveDeleteTarget target, CancellationToken cancellationToken = default)
        {
            var cTarget = (SaveDeleteTarget)target ?? throw new Exception("SaveDeleteTarget must implement SaveDeleteTarget");
            return DoFactoryMethodCall(cTarget, FactoryOperation.Update, () => cTarget.Update());
        }

        public SaveDeleteTarget LocalDelete(SaveDeleteTarget target, CancellationToken cancellationToken = default)
        {
            var cTarget = (SaveDeleteTarget)target ?? throw new Exception("SaveDeleteTarget must implement SaveDeleteTarget");
            return DoFactoryMethodCall(cTarget, FactoryOperation.Delete, () => cTarget.Delete());
        }

        public virtual SaveDeleteTarget? Save(SaveDeleteTarget target, CancellationToken cancellationToken = default)
        {
            return LocalSave(target, cancellationToken);
        }

        public virtual SaveDeleteTarget? LocalSave(SaveDeleteTarget target, CancellationToken cancellationToken = default)
        {
            if (target.IsDeleted)
            {
                if (target.IsNew)
                {
                    return default(SaveDeleteTarget);
                }

                return (LocalDelete(target, cancellationToken))!;
            }
            else if (target.IsNew)
            {
                return (LocalInsert(target, cancellationToken))!;
            }
            else
            {
                return (LocalUpdate(target, cancellationToken))!;
            }
        }

        async Task<IFactorySaveMeta?> IFactorySave<SaveDeleteTarget>.Save(SaveDeleteTarget target, CancellationToken cancellationToken)
        {
            return await Task.FromResult((IFactorySaveMeta? )Save(target, cancellationToken));
        }

        public static void FactoryServiceRegistrar(IServiceCollection services, NeatooFactory remoteLocal)
        {
            services.AddScoped<SaveDeleteTargetFactory>();
            services.AddScoped<ISaveDeleteTargetFactory, SaveDeleteTargetFactory>();
            services.AddScoped<IFactorySave<SaveDeleteTarget>, SaveDeleteTargetFactory>();
            // Event registrations
            if (remoteLocal == NeatooFactory.Remote)
            {
            }

            if (remoteLocal == NeatooFactory.Logical || remoteLocal == NeatooFactory.Server)
            {
            }
        }
    }
}