#nullable enable
using Neatoo.RemoteFactory;
using Neatoo.RemoteFactory.Internal;
using Microsoft.Extensions.DependencyInjection;
using Neatoo.RemoteFactory.FactoryGeneratorTests.Shared;

/*
												READONLY - DO NOT EDIT!!!!
												Generated by Neatoo.RemoteFactory
					*/
namespace Neatoo.RemoteFactory.FactoryGeneratorTests.InterfaceFactory
{
    public interface IAuthorizedServiceAsyncFactory : IAuthorizedServiceAsync
    {
        Task<Authorized> CanGetDataAsync(Guid id);
        Task<Authorized> CanProcessDataAsync(Guid id);
    }

    internal class AuthorizedServiceAsyncFactory : IAuthorizedServiceAsyncFactory
    {
        private readonly IServiceProvider ServiceProvider;
        private readonly IMakeRemoteDelegateRequest? MakeRemoteDelegateRequest;
        // Delegates
        public delegate Task<string> GetDataAsyncDelegate(Guid id);
        public delegate Task<int> ProcessDataAsyncDelegate(Guid id, string data);
        // Delegate Properties to provide Local or Remote fork in execution
        public GetDataAsyncDelegate GetDataAsyncProperty { get; }
        public ProcessDataAsyncDelegate ProcessDataAsyncProperty { get; }

        public AuthorizedServiceAsyncFactory(IServiceProvider serviceProvider)
        {
            this.ServiceProvider = serviceProvider;
            GetDataAsyncProperty = LocalGetDataAsync;
            ProcessDataAsyncProperty = LocalProcessDataAsync;
        }

        public AuthorizedServiceAsyncFactory(IServiceProvider serviceProvider, IMakeRemoteDelegateRequest remoteMethodDelegate)
        {
            this.ServiceProvider = serviceProvider;
            this.MakeRemoteDelegateRequest = remoteMethodDelegate;
            GetDataAsyncProperty = RemoteGetDataAsync;
            ProcessDataAsyncProperty = RemoteProcessDataAsync;
        }

        public virtual Task<string> GetDataAsync(Guid id)
        {
            return GetDataAsyncProperty(id);
        }

        public virtual async Task<string> RemoteGetDataAsync(Guid id)
        {
            return (await MakeRemoteDelegateRequest!.ForDelegate<string>(typeof(GetDataAsyncDelegate), [id]))!;
        }

        public async Task<string> LocalGetDataAsync(Guid id)
        {
            Authorized authorized;
            InterfaceAuthAsync interfaceauthasync = ServiceProvider.GetRequiredService<InterfaceAuthAsync>();
            authorized = await interfaceauthasync.CanExecuteBoolAsync();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = await interfaceauthasync.CanExecuteStringAsync();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = await interfaceauthasync.CanExecuteBoolFailAsync(id);
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = await interfaceauthasync.CanExecuteStringFailAsync(id);
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            InterfaceAuth interfaceauth = ServiceProvider.GetRequiredService<InterfaceAuth>();
            authorized = interfaceauth.CanExecuteBool();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = interfaceauth.CanExecuteString();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = interfaceauth.CanExecuteBoolFail(id);
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = interfaceauth.CanExecuteStringFail(id);
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = interfaceauth.CanReadBool();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            var target = ServiceProvider.GetRequiredService<IAuthorizedServiceAsync>();
            return await target.GetDataAsync(id);
        }

        public virtual Task<int> ProcessDataAsync(Guid id, string data)
        {
            return ProcessDataAsyncProperty(id, data);
        }

        public virtual async Task<int> RemoteProcessDataAsync(Guid id, string data)
        {
            return (await MakeRemoteDelegateRequest!.ForDelegate<int>(typeof(ProcessDataAsyncDelegate), [id, data]))!;
        }

        public async Task<int> LocalProcessDataAsync(Guid id, string data)
        {
            Authorized authorized;
            InterfaceAuthAsync interfaceauthasync = ServiceProvider.GetRequiredService<InterfaceAuthAsync>();
            authorized = await interfaceauthasync.CanExecuteBoolAsync();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = await interfaceauthasync.CanExecuteStringAsync();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = await interfaceauthasync.CanExecuteBoolFailAsync(id);
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = await interfaceauthasync.CanExecuteStringFailAsync(id);
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            InterfaceAuth interfaceauth = ServiceProvider.GetRequiredService<InterfaceAuth>();
            authorized = interfaceauth.CanExecuteBool();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = interfaceauth.CanExecuteString();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = interfaceauth.CanExecuteBoolFail(id);
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = interfaceauth.CanExecuteStringFail(id);
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            authorized = interfaceauth.CanReadBool();
            if (!authorized.HasAccess)
            {
                throw new NotAuthorizedException(authorized);
            }

            var target = ServiceProvider.GetRequiredService<IAuthorizedServiceAsync>();
            return await target.ProcessDataAsync(id, data);
        }

        public virtual Task<Authorized> CanGetDataAsync(Guid id)
        {
            return LocalCanGetDataAsync(id);
        }

        public async Task<Authorized> LocalCanGetDataAsync(Guid id)
        {
            Authorized authorized;
            InterfaceAuthAsync interfaceauthasync = ServiceProvider.GetRequiredService<InterfaceAuthAsync>();
            authorized = await interfaceauthasync.CanExecuteBoolAsync();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = await interfaceauthasync.CanExecuteStringAsync();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = await interfaceauthasync.CanExecuteBoolFailAsync(id);
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = await interfaceauthasync.CanExecuteStringFailAsync(id);
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            InterfaceAuth interfaceauth = ServiceProvider.GetRequiredService<InterfaceAuth>();
            authorized = interfaceauth.CanExecuteBool();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = interfaceauth.CanExecuteString();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = interfaceauth.CanExecuteBoolFail(id);
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = interfaceauth.CanExecuteStringFail(id);
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = interfaceauth.CanReadBool();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            return new Authorized(true);
        }

        public virtual Task<Authorized> CanProcessDataAsync(Guid id)
        {
            return LocalCanProcessDataAsync(id);
        }

        public async Task<Authorized> LocalCanProcessDataAsync(Guid id)
        {
            Authorized authorized;
            InterfaceAuthAsync interfaceauthasync = ServiceProvider.GetRequiredService<InterfaceAuthAsync>();
            authorized = await interfaceauthasync.CanExecuteBoolAsync();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = await interfaceauthasync.CanExecuteStringAsync();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = await interfaceauthasync.CanExecuteBoolFailAsync(id);
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = await interfaceauthasync.CanExecuteStringFailAsync(id);
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            InterfaceAuth interfaceauth = ServiceProvider.GetRequiredService<InterfaceAuth>();
            authorized = interfaceauth.CanExecuteBool();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = interfaceauth.CanExecuteString();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = interfaceauth.CanExecuteBoolFail(id);
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = interfaceauth.CanExecuteStringFail(id);
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            authorized = interfaceauth.CanReadBool();
            if (!authorized.HasAccess)
            {
                return authorized;
            }

            return new Authorized(true);
        }

        public static void FactoryServiceRegistrar(IServiceCollection services, NeatooFactory remoteLocal)
        {
            // On the client the Factory is registered
            // All method calls lead to a remote call
            if (remoteLocal == NeatooFactory.Remote)
            {
                services.AddScoped<IAuthorizedServiceAsync, AuthorizedServiceAsyncFactory>();
                services.AddScoped<IAuthorizedServiceAsyncFactory, AuthorizedServiceAsyncFactory>();
            }

            // On the server the Delegates are registered
            // AuthorizedServiceAsyncFactory is not used
            // IAuthorizedServiceAsync must be registered to actual implementation
            if (remoteLocal == NeatooFactory.Server)
            {
                services.AddScoped<IAuthorizedServiceAsyncFactory, AuthorizedServiceAsyncFactory>();
                services.AddScoped<AuthorizedServiceAsyncFactory>();
                services.AddScoped<GetDataAsyncDelegate>(cc =>
                {
                    var factory = cc.GetRequiredService<IAuthorizedServiceAsyncFactory>();
                    return (Guid id) => factory.GetDataAsync(id);
                });
                services.AddScoped<ProcessDataAsyncDelegate>(cc =>
                {
                    var factory = cc.GetRequiredService<IAuthorizedServiceAsyncFactory>();
                    return (Guid id, string data) => factory.ProcessDataAsync(id, data);
                });
            }
        }
    }
}