#nullable enable
using Neatoo.RemoteFactory;
using Neatoo.RemoteFactory.Internal;
using Microsoft.Extensions.DependencyInjection;
using EmployeeManagement.Domain.Interfaces;

/*
    READONLY - DO NOT EDIT!!!!
    Generated by Neatoo.RemoteFactory
*/
namespace EmployeeManagement.Domain.Aggregates
{
    public interface IDepartmentFactory
    {
        Department Create(CancellationToken cancellationToken = default);
        Task<Department?> Fetch(Guid id, CancellationToken cancellationToken = default);
        Task<Department?> Save(Department target, CancellationToken cancellationToken = default);
    }

    internal class DepartmentFactory : FactorySaveBase<Department>, IFactorySave<Department>, IDepartmentFactory
    {
        private readonly IServiceProvider ServiceProvider;
        private readonly IMakeRemoteDelegateRequest? MakeRemoteDelegateRequest;
        // Delegates
        public delegate Task<Department?> FetchDelegate(Guid id, CancellationToken cancellationToken = default);
        public delegate Task<Department?> SaveDelegate(Department target, CancellationToken cancellationToken = default);
        // Delegate Properties to provide Local or Remote fork in execution
        public FetchDelegate FetchProperty { get; }
        public SaveDelegate SaveProperty { get; }

        public DepartmentFactory(IServiceProvider serviceProvider, IFactoryCore<Department> factoryCore) : base(factoryCore)
        {
            this.ServiceProvider = serviceProvider;
            FetchProperty = LocalFetch;
            SaveProperty = LocalSave;
        }

        public DepartmentFactory(IServiceProvider serviceProvider, IMakeRemoteDelegateRequest remoteMethodDelegate, IFactoryCore<Department> factoryCore) : base(factoryCore)
        {
            this.ServiceProvider = serviceProvider;
            this.MakeRemoteDelegateRequest = remoteMethodDelegate;
            FetchProperty = RemoteFetch;
            SaveProperty = RemoteSave;
        }

        public virtual Department Create(CancellationToken cancellationToken = default)
        {
            return LocalCreate(cancellationToken);
        }

        public Department LocalCreate(CancellationToken cancellationToken = default)
        {
            return DoFactoryMethodCall(FactoryOperation.Create, () => new Department());
        }

        public virtual Task<Department?> Fetch(Guid id, CancellationToken cancellationToken = default)
        {
            return FetchProperty(id, cancellationToken);
        }

        public virtual async Task<Department?> RemoteFetch(Guid id, CancellationToken cancellationToken = default)
        {
            return (await MakeRemoteDelegateRequest!.ForDelegateNullable<Department?>(typeof(FetchDelegate), [id], cancellationToken))!;
        }

        public Task<Department?> LocalFetch(Guid id, CancellationToken cancellationToken = default)
        {
            var target = ServiceProvider.GetRequiredService<Department>();
            var repository = ServiceProvider.GetRequiredService<IDepartmentRepository>();
            return DoFactoryMethodCallBoolAsync(target, FactoryOperation.Fetch, () => target.Fetch(id, repository, cancellationToken));
        }

        public Task<Department> LocalInsert(Department target, CancellationToken cancellationToken = default)
        {
            var cTarget = (Department)target ?? throw new Exception("Department must implement Department");
            var repository = ServiceProvider.GetRequiredService<IDepartmentRepository>();
            return DoFactoryMethodCallAsync(cTarget, FactoryOperation.Insert, () => cTarget.Insert(repository, cancellationToken));
        }

        public Task<Department> LocalUpdate(Department target, CancellationToken cancellationToken = default)
        {
            var cTarget = (Department)target ?? throw new Exception("Department must implement Department");
            var repository = ServiceProvider.GetRequiredService<IDepartmentRepository>();
            return DoFactoryMethodCallAsync(cTarget, FactoryOperation.Update, () => cTarget.Update(repository, cancellationToken));
        }

        public Task<Department> LocalDelete(Department target, CancellationToken cancellationToken = default)
        {
            var cTarget = (Department)target ?? throw new Exception("Department must implement Department");
            var repository = ServiceProvider.GetRequiredService<IDepartmentRepository>();
            return DoFactoryMethodCallAsync(cTarget, FactoryOperation.Delete, () => cTarget.Delete(repository, cancellationToken));
        }

        public virtual Task<Department?> Save(Department target, CancellationToken cancellationToken = default)
        {
            return SaveProperty(target, cancellationToken);
        }

        public virtual async Task<Department?> RemoteSave(Department target, CancellationToken cancellationToken = default)
        {
            return (await MakeRemoteDelegateRequest!.ForDelegateNullable<Department?>(typeof(SaveDelegate), [target], cancellationToken))!;
        }

        public virtual Task<Department?> LocalSave(Department target, CancellationToken cancellationToken = default)
        {
            if (target.IsDeleted)
            {
                if (target.IsNew)
                {
                    return Task.FromResult(default(Department));
                }

                return (LocalDelete(target, cancellationToken))!;
            }
            else if (target.IsNew)
            {
                return (LocalInsert(target, cancellationToken))!;
            }
            else
            {
                return (LocalUpdate(target, cancellationToken))!;
            }
        }

        async Task<IFactorySaveMeta?> IFactorySave<Department>.Save(Department target, CancellationToken cancellationToken)
        {
            return (IFactorySaveMeta? )await Save(target, cancellationToken);
        }

        public static void FactoryServiceRegistrar(IServiceCollection services, NeatooFactory remoteLocal)
        {
            services.AddScoped<DepartmentFactory>();
            services.AddScoped<IDepartmentFactory, DepartmentFactory>();
            services.AddTransient<Department>();
            services.AddScoped<IFactorySave<Department>, DepartmentFactory>();
            // Register AOT-compatible ordinal converter
            global::Neatoo.RemoteFactory.Internal.NeatooOrdinalConverterFactory.RegisterConverter(Department.CreateOrdinalConverter());
            services.AddScoped<FetchDelegate>(cc =>
            {
                var factory = cc.GetRequiredService<DepartmentFactory>();
                return (Guid id, CancellationToken cancellationToken = default) => factory.LocalFetch(id, cancellationToken);
            });
            services.AddScoped<SaveDelegate>(cc =>
            {
                var factory = cc.GetRequiredService<DepartmentFactory>();
                return (Department target, CancellationToken cancellationToken = default) => factory.LocalSave(target, cancellationToken);
            });
            // Event registrations
            if (remoteLocal == NeatooFactory.Remote)
            {
            }

            if (remoteLocal == NeatooFactory.Logical || remoteLocal == NeatooFactory.Server)
            {
            }
        }
    }
}