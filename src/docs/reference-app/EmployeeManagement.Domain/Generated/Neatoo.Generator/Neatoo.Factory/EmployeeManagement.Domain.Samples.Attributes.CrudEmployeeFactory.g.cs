#nullable enable
using Neatoo.RemoteFactory;
using Neatoo.RemoteFactory.Internal;
using Microsoft.Extensions.DependencyInjection;
using EmployeeManagement.Domain.Interfaces;
using EmployeeManagement.Domain.Samples.Authorization;

/*
    READONLY - DO NOT EDIT!!!!
    Generated by Neatoo.RemoteFactory
*/
namespace EmployeeManagement.Domain.Samples.Attributes
{
    public interface ICrudEmployeeFactory
    {
        CrudEmployee Create(CancellationToken cancellationToken = default);
        Task<CrudEmployee?> Fetch(Guid id, CancellationToken cancellationToken = default);
        Task<CrudEmployee?> Save(CrudEmployee target, CancellationToken cancellationToken = default);
    }

    internal class CrudEmployeeFactory : FactorySaveBase<CrudEmployee>, IFactorySave<CrudEmployee>, ICrudEmployeeFactory
    {
        private readonly IServiceProvider ServiceProvider;
        private readonly IMakeRemoteDelegateRequest? MakeRemoteDelegateRequest;
        // Delegates
        public delegate Task<CrudEmployee?> FetchDelegate(Guid id, CancellationToken cancellationToken = default);
        public delegate Task<CrudEmployee?> SaveDelegate(CrudEmployee target, CancellationToken cancellationToken = default);
        // Delegate Properties to provide Local or Remote fork in execution
        public FetchDelegate FetchProperty { get; }
        public SaveDelegate SaveProperty { get; }

        public CrudEmployeeFactory(IServiceProvider serviceProvider, IFactoryCore<CrudEmployee> factoryCore) : base(factoryCore)
        {
            this.ServiceProvider = serviceProvider;
            FetchProperty = LocalFetch;
            SaveProperty = LocalSave;
        }

        public CrudEmployeeFactory(IServiceProvider serviceProvider, IMakeRemoteDelegateRequest remoteMethodDelegate, IFactoryCore<CrudEmployee> factoryCore) : base(factoryCore)
        {
            this.ServiceProvider = serviceProvider;
            this.MakeRemoteDelegateRequest = remoteMethodDelegate;
            FetchProperty = RemoteFetch;
            SaveProperty = RemoteSave;
        }

        public virtual CrudEmployee Create(CancellationToken cancellationToken = default)
        {
            return LocalCreate(cancellationToken);
        }

        public CrudEmployee LocalCreate(CancellationToken cancellationToken = default)
        {
            return DoFactoryMethodCall(FactoryOperation.Create, () => new CrudEmployee());
        }

        public virtual Task<CrudEmployee?> Fetch(Guid id, CancellationToken cancellationToken = default)
        {
            return FetchProperty(id, cancellationToken);
        }

        public virtual async Task<CrudEmployee?> RemoteFetch(Guid id, CancellationToken cancellationToken = default)
        {
            return (await MakeRemoteDelegateRequest!.ForDelegateNullable<CrudEmployee?>(typeof(FetchDelegate), [id], cancellationToken))!;
        }

        public Task<CrudEmployee?> LocalFetch(Guid id, CancellationToken cancellationToken = default)
        {
            var target = ServiceProvider.GetRequiredService<CrudEmployee>();
            var repo = ServiceProvider.GetRequiredService<IEmployeeRepository>();
            return DoFactoryMethodCallBoolAsync(target, FactoryOperation.Fetch, () => target.Fetch(id, repo, cancellationToken));
        }

        public Task<CrudEmployee> LocalInsert(CrudEmployee target, CancellationToken cancellationToken = default)
        {
            var cTarget = (CrudEmployee)target ?? throw new Exception("CrudEmployee must implement CrudEmployee");
            var repo = ServiceProvider.GetRequiredService<IEmployeeRepository>();
            return DoFactoryMethodCallAsync(cTarget, FactoryOperation.Insert, () => cTarget.Insert(repo, cancellationToken));
        }

        public Task<CrudEmployee> LocalUpdate(CrudEmployee target, CancellationToken cancellationToken = default)
        {
            var cTarget = (CrudEmployee)target ?? throw new Exception("CrudEmployee must implement CrudEmployee");
            var repo = ServiceProvider.GetRequiredService<IEmployeeRepository>();
            return DoFactoryMethodCallAsync(cTarget, FactoryOperation.Update, () => cTarget.Update(repo, cancellationToken));
        }

        public Task<CrudEmployee> LocalDelete(CrudEmployee target, CancellationToken cancellationToken = default)
        {
            var cTarget = (CrudEmployee)target ?? throw new Exception("CrudEmployee must implement CrudEmployee");
            var repo = ServiceProvider.GetRequiredService<IEmployeeRepository>();
            return DoFactoryMethodCallAsync(cTarget, FactoryOperation.Delete, () => cTarget.Delete(repo, cancellationToken));
        }

        public virtual Task<CrudEmployee?> Save(CrudEmployee target, CancellationToken cancellationToken = default)
        {
            return SaveProperty(target, cancellationToken);
        }

        public virtual async Task<CrudEmployee?> RemoteSave(CrudEmployee target, CancellationToken cancellationToken = default)
        {
            return (await MakeRemoteDelegateRequest!.ForDelegateNullable<CrudEmployee?>(typeof(SaveDelegate), [target], cancellationToken))!;
        }

        public virtual Task<CrudEmployee?> LocalSave(CrudEmployee target, CancellationToken cancellationToken = default)
        {
            if (target.IsDeleted)
            {
                if (target.IsNew)
                {
                    return Task.FromResult(default(CrudEmployee));
                }

                return (LocalDelete(target, cancellationToken))!;
            }
            else if (target.IsNew)
            {
                return (LocalInsert(target, cancellationToken))!;
            }
            else
            {
                return (LocalUpdate(target, cancellationToken))!;
            }
        }

        async Task<IFactorySaveMeta?> IFactorySave<CrudEmployee>.Save(CrudEmployee target, CancellationToken cancellationToken)
        {
            return (IFactorySaveMeta? )await Save(target, cancellationToken);
        }

        public static void FactoryServiceRegistrar(IServiceCollection services, NeatooFactory remoteLocal)
        {
            services.AddScoped<CrudEmployeeFactory>();
            services.AddScoped<ICrudEmployeeFactory, CrudEmployeeFactory>();
            services.AddTransient<CrudEmployee>();
            services.AddScoped<IFactorySave<CrudEmployee>, CrudEmployeeFactory>();
            // Register AOT-compatible ordinal converter
            global::Neatoo.RemoteFactory.Internal.NeatooOrdinalConverterFactory.RegisterConverter(CrudEmployee.CreateOrdinalConverter());
            services.AddScoped<FetchDelegate>(cc =>
            {
                var factory = cc.GetRequiredService<CrudEmployeeFactory>();
                return (Guid id, CancellationToken cancellationToken = default) => factory.LocalFetch(id, cancellationToken);
            });
            services.AddScoped<SaveDelegate>(cc =>
            {
                var factory = cc.GetRequiredService<CrudEmployeeFactory>();
                return (CrudEmployee target, CancellationToken cancellationToken = default) => factory.LocalSave(target, cancellationToken);
            });
            // Event registrations
            if (remoteLocal == NeatooFactory.Remote)
            {
            }

            if (remoteLocal == NeatooFactory.Logical || remoteLocal == NeatooFactory.Server)
            {
            }
        }
    }
}